# Firebase Cloud Functions — local emulator secrets
# Copy to `functions/.env` for local development.
# For production, configure via Firebase Console or CLI.

RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

# Optional: enforce Firebase App Check on callable functions
# ENFORCE_APP_CHECK=true
